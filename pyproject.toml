[project]
name = "FSKit"
version = "0.4.0.0"
description = "Free Signage Kit"
requires-python = ">=3.12"
dependencies = [
    "pyside6",
    "packaging",
    "nuitka",
]

[dependency-groups]
dev = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv]
package = false